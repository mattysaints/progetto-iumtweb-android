<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single" android:id="@+id/navApp">
        <item
            android:id="@+id/nav_home"
            android:icon="@drawable/ic_home"
            android:title="@string/menu_home" />
        <item
            android:id="@+id/nav_gallery"
            android:icon="@drawable/ic_prenotare"
            android:title="Prenota una ripetizione" />
        <item
            android:id="@+id/nav_slideshow"
            android:icon="@drawable/ic_visualizza"
            android:title="Visualizza le tue prenotazioni" />
    </group>

    <group android:checkableBehavior="single" android:id="@+id/navLogout">
        <item
            android:id="@+id/nav_logout"
            android:icon="@drawable/ic_logout"
            android:onClick="gotoLogin"
            android:title="Logout" />
    </group>

</menu>
